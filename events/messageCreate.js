module.exports = {
    once: false,
    async execute(message, client) {
        if (message.author.bot) return;

        /**
         * To-Do:
         * Add chat system through bot dms that is monitored and filtered
         * 
         */
    }
}